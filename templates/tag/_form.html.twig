

{{ form_start(form) }}
    	{{ form_row(form.name, {
            'attr': {'class': 'form-control mt-2 mb-4'},
            label: 'Tag name'
        }) }}
    <button class="btn new-data-btn">{{ button_label|default('Confirm') }}</button>
{{ form_end(form) }}
